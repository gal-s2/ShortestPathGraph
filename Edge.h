#ifndef EDGE_H
#define EDGE_H

class Edge {
public:
    int source, dest; // Source and destination nodes of the edge.

    Edge(int source, int dest); // Constructor to initialize an edge with a source and destination.
};

#endif //EDGE_H
